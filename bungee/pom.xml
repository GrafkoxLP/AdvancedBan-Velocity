<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>me.leoko.advancedban</groupId>
        <artifactId>AdvancedBan</artifactId>
        <version>2.4.0</version>
    </parent>

    <artifactId>AdvancedBan-Bungee</artifactId>
    <packaging>jar</packaging>

    <repositories>
        <!--
          Order matters! Maven queries repos in declared order and stops at the first
          successful (or fatally-failing) response. CodeMC has occasionally served
          truncated artifacts for net.md-5:bungeecord-* — putting PaperMC first
          (which mirrors both Bungee and bStats reliably) avoids that path.
        -->
        <!-- PaperMC: mirrors Bungee snapshots AND bStats reliably -->
        <repository>
            <id>papermc</id>
            <url>https://repo.papermc.io/repository/maven-public/</url>
        </repository>
        <!-- Sonatype: official source for BungeeCord snapshots -->
        <repository>
            <id>bungeecord-repo</id>
            <url>https://oss.sonatype.org/content/repositories/snapshots</url>
        </repository>
        <!-- CodeMC: last-resort fallback for bStats -->
        <repository>
            <id>CodeMC</id>
            <url>https://repo.codemc.org/repository/maven-public</url>
        </repository>
    </repositories>

    <dependencies>
        <dependency>
            <groupId>net.md-5</groupId>
            <artifactId>bungeecord-api</artifactId>
            <!--
              BungeeCord snapshots can be removed/replaced on Sonatype. 1.20-R0.2-SNAPSHOT
              has been the most consistently available version. The API surface AdvancedBan
              uses (LoginEvent, ChatEvent, ProxiedPlayer, ...) is stable across 1.8-1.21,
              so this works fine on modern BungeeCord installations too.
            -->
            <version>1.20-R0.2-SNAPSHOT</version>
            <scope>provided</scope>
        </dependency>

        <dependency>
            <groupId>net.luckperms</groupId>
            <artifactId>api</artifactId>
            <version>5.4</version>
            <scope>provided</scope>
        </dependency>

        <dependency>
            <groupId>org.bstats</groupId>
            <artifactId>bstats-bungeecord</artifactId>
            <version>3.1.0</version>
            <scope>compile</scope>
        </dependency>

        <dependency>
            <groupId>me.leoko.advancedban</groupId>
            <artifactId>AdvancedBan-Core</artifactId>
            <version>${project.version}</version>
            <scope>compile</scope>
        </dependency>
    </dependencies>

</project>
